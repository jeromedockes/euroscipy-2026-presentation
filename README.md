Needs quarto, skrub main branch (not stable release), jupyter notebook and
jupytext installed.

generate the presentation & the notebooks with `make` or preview the
presentation with `make preview`.

notebooks to be shown are `intro.ipynb`, `tuning.ipynb` and
`electricity_forecasting_example.ipynb`.
